Two hero layers for the cursor spotlight, same dimensions, same shot in two states:
  hero-base.webp    always visible
  hero-reveal.webp  only visible inside the spotlight

Optional project screenshots: point Project.image in src/content/content.ts at them.

With no images here the spotlight inverts the live 3D scene instead.
